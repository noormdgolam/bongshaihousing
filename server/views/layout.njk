<!DOCTYPE html>
<html lang="en">
<head>
<script>(function(){try{if(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches){document.documentElement.setAttribute('data-theme','dark');}}catch(e){}})();</script>
{% block head_top %}{% endblock %}
  <link rel="dns-prefetch" href="https://fonts.googleapis.com">
  <link rel="dns-prefetch" href="https://fonts.gstatic.com">
  <link rel="preload" href="css/style.min.css?v=4.10" as="style">
  <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"></noscript>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}" />
  {% if keywords %}<meta name="keywords" content="{{ keywords }}" />{% endif %}
  <meta name="author" content="Bongshai Housing Ltd." />
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
  <meta name="revisit-after" content="7 days" />
  <meta name="language" content="English" />
  {% if category %}<meta name="category" content="{{ category }}" />{% endif %}
  <meta name="geo.region" content="BD-C" />
  <meta name="geo.placename" content="Uttara, Dhaka, Bangladesh" />
  <meta name="geo.position" content="23.8728;90.3984" />
  <meta name="ICBM" content="23.8728, 90.3984" />
  <link rel="canonical" href="{{ canonical }}" />
  <meta property="og:type" content="{{ ogType or 'website' }}" />
  <meta property="og:title" content="{{ ogTitle or title }}" />
  <meta property="og:description" content="{{ ogDescription or description }}" />
  <meta property="og:image" content="{{ ogImage or 'https://bongshaihousing.com/images/logo-icon-512.webp' }}" />
  <meta property="og:image:width" content="{{ ogImageWidth or 512 }}" />
  <meta property="og:image:height" content="{{ ogImageHeight or 512 }}" />
  <meta property="og:url" content="{{ canonical }}" />
  <meta property="og:site_name" content="Bongshai Housing" />
  <meta property="og:locale" content="en_BD" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="{{ twitterTitle or ogTitle or title }}" />
  <meta name="twitter:description" content="{{ twitterDescription or ogDescription or description }}" />
  <meta name="twitter:image" content="{{ ogImage or 'https://bongshaihousing.com/images/logo-icon-512.webp' }}" />
  <link rel="stylesheet" href="css/style.min.css?v=4.10" />
  {% block head_extra %}{% endblock %}
  <link rel="alternate" hreflang="en-BD" href="{{ canonical }}" />
  <link rel="alternate" hreflang="x-default" href="{{ canonical }}" />
  <meta name="apple-mobile-web-app-capable" content="yes">
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="{{ theme.primary or '#1E40AF' }}">
  <link rel="apple-touch-icon" href="images/logo-icon-192.webp">
  <link rel="icon" href="/favicon.ico" sizes="any">

  <!-- Dynamic Theme Design Tokens & Custom Properties -->
  <style id="bh-theme-custom-vars">
    :root {
      {% if themeCssVars %}{{ themeCssVars | safe }}{% endif %}
    }
  </style>
  <style id="bh-theme-announcement-styles">
    .bh-top-announcement {
      font-size: 0.82rem;
      padding: 7px 16px;
      text-align: center;
      font-weight: 600;
      position: relative;
      z-index: 1000;
      letter-spacing: 0.1px;
      transition: all 0.2s ease;
    }
    .bh-announcement-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
    }
    .bh-announcement-cta {
      text-decoration: underline !important;
      font-weight: 700;
      margin-left: 6px;
    }
    .bh-announcement-cta:hover {
      opacity: 0.9;
    }
  </style>
  {% if theme.custom_css %}
  <style id="bh-theme-custom-css">
    {{ theme.custom_css | safe }}
  </style>
  {% endif %}
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>
<a href="#main-content" class="skip-link">Skip to content</a>

<!-- Top Announcement Bar -->
<div id="bhTopAnnouncement" class="bh-top-announcement" style="background: {{ theme.announcement_bg or '#1E3A8A' }}; color: {{ theme.announcement_color or '#FDE047' }}; {% if not theme.show_announcement %}display:none;{% endif %}">
  <div class="bh-announcement-inner">
    <span id="bhAnnouncementText">{{ theme.announcement_text or '⚡ দ্রুততম সময়ে ৪৫-৬০ দিনে ভূমিকম্প সহনশীল প্রি-ইঞ্জিনিয়ার্ড স্টিল বাড়ি নির্মাণ — ফ্রি পরামর্শ নিন!' }}</span>
    {% if theme.announcement_link %}
    <a href="{{ theme.announcement_link }}" id="bhAnnouncementLink" class="bh-announcement-cta" style="color: {{ theme.announcement_color or '#FDE047' }};">বিস্তারিত জানুন →</a>
    {% endif %}
  </div>
</div>

{% include "partials/nav.njk" %}

<main id="main-content">
{% block content %}{% endblock %}
</main>
{% block content_after %}{% endblock %}

<footer class="footer" role="contentinfo">
<div class="container">
<div class="footer-grid">
<!-- Brand Col -->
<div class="footer-brand">
<a class="navbar-brand" href="index.html" style="margin-bottom:var(--space-4);display:inline-flex">
<div class="brand-logo-wrap">
<svg aria-hidden="true" viewbox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"></path></svg>
</div>
<div class="brand-text">
<span class="brand-name">BongshaiHousing</span>
<span class="brand-tagline">Real Estate &amp; Developer</span>
</div>
</a>
<p class="footer-brand-desc">Bangladesh's premier real estate and prefab housing company. We build dreams with international quality, innovative technology, and unwavering commitment to excellence.</p>
<div aria-label="Social media links" class="footer-social">
<a aria-label="WhatsApp" class="social-btn" href="https://wa.me/8801781636613" rel="noopener noreferrer">💬</a>
<a aria-label="Facebook" class="social-btn" href="https://www.facebook.com/bongshaihousing" rel="noopener noreferrer">📘</a>
<a aria-label="Email" class="social-btn" href="mailto:sales@bongshai.com" itemprop="email">✉️</a>
<a aria-label="Phone" class="social-btn" href="tel:+8801781636613" itemprop="telephone" content="+8801781636613">📞</a>
<a aria-label="Find us on Google" class="social-btn" href="https://share.google/o4qfoSNEk5eRdxzvv" rel="noopener noreferrer" target="_blank">📍</a>
</div>
</div>
<!-- Quick Links -->
<div>
<h3 class="footer-col-title">Quick Links</h3>
<nav aria-label="Footer navigation" class="footer-links">
<a class="footer-link" href="index.html">Home</a>
<a class="footer-link" href="about.html">About Company</a>
<a class="footer-link" href="products-and-solutions.html">Products</a>
<a class="footer-link" href="faq.html">FAQ</a>
<a class="footer-link" href="gallery.html">Gallery</a>
<a class="footer-link" href="contact.html">Contact Us</a>

  <h4 style="margin-top:20px; color:white; font-size:1.1rem;">Sister Companies</h4>
  <a class="footer-link" href="https://bongshaisteel.com/" target="_blank">Bongshai Steel</a>
  <a class="footer-link" href="https://bongshaiengineering.com/" target="_blank">Bongshai Engineering</a>
  </nav>
  </div>
<!-- Service Areas -->
<div>
<h3 class="footer-col-title">Service Areas</h3>
<nav aria-label="Service Areas" class="footer-links">
<a class="footer-link" href="service-areas.html" style="color:var(--accent-light); font-weight:700;">🌐 View All 64 Districts</a>
<a class="footer-link" href="steel-building-dhaka.html">Dhaka &amp; Probachol</a>
<a class="footer-link" href="steel-building-chotrogram.html">Chattogram</a>
<a class="footer-link" href="steel-building-gazipur.html">Gazipur</a>
<a class="footer-link" href="prefab-housing-cumilla.html">Cumilla</a>
<a class="footer-link" href="prefab-cottage-bogra-rangpur.html">Bogra &amp; Rangpur</a>
</nav>
</div>
<!-- Contact -->
<div>
<h3 class="footer-col-title">Contact Info</h3>
<address class="footer-contact-items" itemscope="" itemtype="http://schema.org/LocalBusiness" style="font-style:normal">
<span itemprop="name" style="display:none;">Bongshai Housing Ltd.</span>
<div class="footer-contact-item" itemprop="address" itemscope="" itemtype="http://schema.org/PostalAddress">
<span aria-hidden="true" class="footer-contact-icon">📍</span>
<span>
<span itemprop="streetAddress">House #18, Road #18, Sector #10</span>,
                <span itemprop="addressLocality">Uttara</span>, <span itemprop="addressRegion">Dhaka</span> –
                <span itemprop="postalCode">1230</span>
<span itemprop="addressCountry" style="display:none;">BD</span>
</span>
</div>
<div class="footer-contact-item">
<span aria-hidden="true" class="footer-contact-icon">📞</span>
<span><a href="tel:+8801781636613" itemprop="telephone" style="color:inherit">01781-636613</a><br/><a href="tel:+8801714104940" style="color:inherit">01714-104940</a></span>
</div>
<div class="footer-contact-item">
<span aria-hidden="true" class="footer-contact-icon">✉️</span>
<span><a href="mailto:sales@bongshai.com" itemprop="email" style="color:inherit">sales@bongshai.com</a></span>
</div>
<div class="footer-contact-item">
<span aria-hidden="true" class="footer-contact-icon">⏰</span>
<span>Sat – Thu: 9:00 AM – 7:00 PM</span>
</div>
</address>
<!-- Local SEO Map Embed -->
<div style="margin-top: 1.5rem; border-radius: 8px; overflow: hidden; height: 150px; border: 1px solid rgba(255,255,255,0.1);">
<iframe allowfullscreen="" height="100%" loading="lazy" referrerpolicy="no-referrer-when-downgrade" src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3648.435773822986!2d90.395825!3d23.874158!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3755c43d7ab09001%3A0xcb1b7cb0e86b29d4!2sSector%2010%2C%20Uttara%2C%20Dhaka%201230!5e0!3m2!1sen!2sbd!4v1700000000000!5m2!1sen!2sbd" style="border:0;" title="Bongshai Housing Location" width="100%"></iframe>
</div>
</div>
</div>
<div class="footer-bottom">
{% block footer_bottom_extra %}{% endblock %}
<p class="footer-copy">© <span id="year"></span> Bongshai Housing Ltd. All Rights Reserved. | Dhaka, Bangladesh</p>
<p class="footer-copy" style="margin-top:8px;">Part of the Bongshai Group — see also <a href="https://bongshaisteel.com/" target="_blank" rel="noopener" style="color:var(--accent-light); font-weight:700;">Bongshai Steel</a></p>
<nav aria-label="Legal links" class="footer-legal">
<a href="privacy-policy.html">Privacy Policy</a>
<a href="terms.html">Terms &amp; Conditions</a>
<a href="sitemap.xml">Sitemap</a>
</nav>
</div>
</div>
</footer>

  <a href="{{ whatsappHref or 'https://wa.me/8801781636613?text=Hello%20Bongshai%20Housing%2C%20I%20have%20a%20question.' }}" class="whatsapp-float" aria-label="Chat on WhatsApp" target="_blank" rel="noopener noreferrer">
    <div class="whatsapp-float-pulse" aria-hidden="true"></div>💬
  </a>

  {% include "partials/ai-chat-widget.njk" %}

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
    const nav = document.getElementById('mainNav');
    window.addEventListener('scroll', () => { nav.classList.toggle('scrolled', window.scrollY > 20); }, { passive: true });
    const hamburger = document.getElementById('hamburgerBtn');
    const drawer = document.getElementById('mobileDrawer');
    hamburger.addEventListener('click', () => { const isOpen = drawer.classList.toggle('open'); hamburger.classList.toggle('open', isOpen); hamburger.setAttribute('aria-expanded', isOpen); document.body.style.overflow = isOpen ? 'hidden' : ''; });
    // Category sidebar mobile toggle
    (function() {
      var toggle = document.getElementById('catSidebarToggle');
      var body = document.getElementById('catSidebarBody');
      if (toggle && body) {
        toggle.addEventListener('click', function() {
          var isOpen = body.classList.toggle('open');
          toggle.classList.toggle('open', isOpen);
          toggle.setAttribute('aria-expanded', isOpen);
        });
      }
    })();
    const observer = new IntersectionObserver((entries) => { entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); observer.unobserve(e.target); } }); }, { threshold: 0.12 });
    document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => observer.observe(el));
  </script>

{% block aos_script %}
<script src="js/aos.js"></script>
<script>AOS.init({ once: true, duration: 800, offset: 50 });</script>
{% endblock %}
<script>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
      navigator.serviceWorker.register('/sw.js').then(registration => {
        console.log('SW registered: ', registration);
      }).catch(registrationError => {
        console.log('SW registration failed: ', registrationError);
      });
    });
  }
</script>
<script src="js/3d-tilt.js?v=1" defer></script>
<script src="js/page-transition.js?v=1" defer></script>
<script src="js/global-upgrades.min.js?v=3.13" defer></script>

<!-- Live Theme Editor postMessage Listener & Visual Pinpoint Engine -->
<script>
(function() {
  let activeOverlays = [];
  let hideTimeout = null;

  function clearHighlight() {
    if (hideTimeout) clearTimeout(hideTimeout);
    activeOverlays.forEach(function(el) { el.remove(); });
    activeOverlays = [];
  }

  function getElementsForTarget(target) {
    var primary = null;
    var all = [];
    switch (target) {
      case 'primary':
      case 'primary_dark':
      case 'primary_light':
        primary = document.querySelector('.btn-primary, .page-hero .btn, .navbar-brand, .theme-primary-bg, button[type="submit"]') || document.querySelector('.navbar');
        all = Array.from(document.querySelectorAll('.btn-primary, .page-hero .btn, .navbar-brand, .theme-primary-bg, button[type="submit"], .stat-card, .footer-col h3, .nav-cta'));
        break;
      case 'accent':
      case 'accent_light':
      case 'accent_dark':
        primary = document.querySelector('.page-hero-label, .badge, .property-type, h1 span, .text-accent, .phone-btn') || document.querySelector('.page-hero-title');
        all = Array.from(document.querySelectorAll('.page-hero-label, .badge, .property-type, h1 span, h2 span, .text-accent, .phone-btn, .trust-signals strong, .cta-badge'));
        break;
      case 'heading_font':
      case 'font_heading':
        primary = document.querySelector('h1, .page-hero-title') || document.querySelector('h2');
        all = Array.from(document.querySelectorAll('h1, h2, h3, .page-hero-title, .section-title, .hero-title'));
        break;
      case 'body_font':
      case 'font_body':
        primary = document.querySelector('.page-hero-text, p.lead, main p') || document.querySelector('p');
        all = Array.from(document.querySelectorAll('.page-hero-text, p.lead, main p, .container p, .hero-text'));
        break;
      case 'announcement':
      case 'show_announcement':
      case 'announcement_text':
      case 'announcement_link':
      case 'announcement_bg':
      case 'announcement_color':
        primary = document.getElementById('bhTopAnnouncement') || document.querySelector('header, .navbar');
        all = primary ? [primary] : [];
        break;
      case 'radius':
      case 'radius_md':
      case 'radius_style':
        primary = document.querySelector('.btn-primary, .btn, .card, .page-hero-label') || document.querySelector('.btn');
        all = Array.from(document.querySelectorAll('.btn, .card, .page-hero-label, input, .property-card, .stat-card, .btn-primary'));
        break;
      case 'preset':
        primary = document.querySelector('.page-hero, .navbar') || document.body;
        all = Array.from(document.querySelectorAll('.btn-primary, .page-hero-label, h1 span, .navbar'));
        break;
      case 'custom_css':
        primary = document.querySelector('main, .page-hero') || document.body;
        all = primary ? [primary] : [];
        break;
      default:
        primary = null;
        all = [];
    }
    return { primary: primary, all: all };
  }

  function showHighlight(target, label) {
    clearHighlight();
    if (!target) return;

    const data = getElementsForTarget(target);
    const mainEl = data.primary || (data.all.length > 0 ? data.all[0] : null);
    if (!mainEl) return;

    // 1. Smooth scroll to primary element
    if (target.includes('announcement')) {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    } else if (typeof mainEl.scrollIntoView === 'function') {
      mainEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }

    // 2. Setup styles for laser pulse & tag
    const isAccent = target.includes('accent');
    const borderColor = isAccent ? '#F59E0B' : '#38BDF8';
    const bgGlow = isAccent ? 'rgba(245, 158, 11, 0.18)' : 'rgba(56, 189, 248, 0.18)';
    const shadowColor = isAccent ? 'rgba(245, 158, 11, 0.55)' : 'rgba(56, 189, 248, 0.55)';

    if (!document.getElementById('bh-theme-pinpoint-styles')) {
      const pinStyle = document.createElement('style');
      pinStyle.id = 'bh-theme-pinpoint-styles';
      pinStyle.innerHTML = `
        @keyframes bhLaserPulse {
          0% { box-shadow: 0 0 0 0 rgba(56, 189, 248, 0.7), 0 0 15px rgba(56, 189, 248, 0.4); }
          50% { box-shadow: 0 0 0 8px rgba(56, 189, 248, 0), 0 0 25px rgba(56, 189, 248, 0.8); }
          100% { box-shadow: 0 0 0 0 rgba(56, 189, 248, 0.7), 0 0 15px rgba(56, 189, 248, 0.4); }
        }
        @keyframes bhTagBounce {
          0%, 100% { transform: translateY(0); }
          50% { transform: translateY(-4px); }
        }
        .bh-pinpoint-box {
          position: absolute;
          pointer-events: none;
          z-index: 999999;
          border-radius: 8px;
          transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
          animation: bhLaserPulse 1.8s infinite;
        }
        .bh-pinpoint-tag {
          position: absolute;
          top: -30px;
          left: 4px;
          background: #0B1120;
          color: #38BDF8;
          font-family: 'Inter', system-ui, -apple-system, sans-serif;
          font-size: 11px;
          font-weight: 700;
          letter-spacing: 0.3px;
          padding: 4px 10px;
          border-radius: 20px;
          border: 1px solid #38BDF8;
          box-shadow: 0 4px 14px rgba(0,0,0,0.6);
          display: flex;
          align-items: center;
          gap: 5px;
          white-space: nowrap;
          animation: bhTagBounce 2s infinite ease-in-out;
        }
        .bh-pinpoint-secondary {
          position: absolute;
          pointer-events: none;
          z-index: 999998;
          border-radius: 6px;
          border: 2px dashed rgba(56, 189, 248, 0.7);
          background: rgba(56, 189, 248, 0.08);
          transition: all 0.2s ease;
        }
      `;
      document.head.appendChild(pinStyle);
    }

    function createBox(el, isMain) {
      if (!el) return null;
      const rect = el.getBoundingClientRect();
      if (rect.width === 0 && rect.height === 0) return null;

      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
      const scrollLeft = window.pageXOffset || document.documentElement.scrollLeft;

      const box = document.createElement('div');
      box.className = isMain ? 'bh-pinpoint-box' : 'bh-pinpoint-secondary';
      
      const pad = 4;
      box.style.top = (rect.top + scrollTop - pad) + 'px';
      box.style.left = (rect.left + scrollLeft - pad) + 'px';
      box.style.width = (rect.width + pad * 2) + 'px';
      box.style.height = (rect.height + pad * 2) + 'px';
      box.style.borderColor = borderColor;
      box.style.background = isMain ? bgGlow : 'rgba(56, 189, 248, 0.06)';

      if (isMain) {
        box.style.border = '2.5px solid ' + borderColor;
        box.style.boxShadow = '0 0 0 3px ' + bgGlow + ', 0 0 20px ' + shadowColor;
        if (label) {
          const tag = document.createElement('div');
          tag.className = 'bh-pinpoint-tag';
          tag.style.borderColor = borderColor;
          tag.style.color = isAccent ? '#FDE047' : '#38BDF8';
          tag.innerHTML = '<span style="font-size:12px;">📍</span> <span>' + label + '</span>';
          box.appendChild(tag);
        }
      }

      document.body.appendChild(box);
      activeOverlays.push(box);
      return box;
    }

    createBox(mainEl, true);

    // Secondary highlights for visible elements
    if (data.all && data.all.length > 1) {
      let count = 0;
      data.all.forEach(function(el) {
        if (el !== mainEl && count < 5) {
          const rect = el.getBoundingClientRect();
          if (rect.top >= -200 && rect.bottom <= window.innerHeight + 400) {
            createBox(el, false);
            count++;
          }
        }
      });
    }

    hideTimeout = setTimeout(clearHighlight, 6000);
  }

  // Window message router
  window.addEventListener('message', function(event) {
    if (!event.data) return;

    if (event.data.type === 'BH_THEME_UPDATE') {
      const theme = event.data.theme;
      if (!theme) return;

      // 1. Update CSS Custom Properties in 0ms real-time
      const styleEl = document.getElementById('bh-theme-custom-vars');
      if (styleEl) {
        styleEl.innerHTML = `
          :root {
            --primary: ${theme.primary};
            --primary-dark: ${theme.primary_dark};
            --primary-light: ${theme.primary_light};
            --accent: ${theme.accent};
            --accent-light: ${theme.accent_light};
            --accent-dark: ${theme.accent_dark};
            --font-heading: ${theme.font_heading};
            --font-body: ${theme.font_body};
            --radius-sm: ${theme.radius_sm};
            --radius-md: ${theme.radius_md};
            --radius-lg: ${theme.radius_lg};
            --radius-xl: ${theme.radius_xl};
          }
        `;
      }

      // 2. Update Top Announcement Banner in real-time
      const banner = document.getElementById('bhTopAnnouncement');
      const bannerText = document.getElementById('bhAnnouncementText');
      const bannerLink = document.getElementById('bhAnnouncementLink');
      if (banner) {
        banner.style.display = theme.show_announcement ? 'block' : 'none';
        banner.style.backgroundColor = theme.announcement_bg || '#1E3A8A';
        banner.style.color = theme.announcement_color || '#FDE047';
      }
      if (bannerText && theme.announcement_text) {
        bannerText.textContent = theme.announcement_text;
      }
      if (bannerLink) {
        bannerLink.style.color = theme.announcement_color || '#FDE047';
        if (theme.announcement_link) bannerLink.setAttribute('href', theme.announcement_link);
      }

      // 3. Update Custom CSS in real-time
      let customCssEl = document.getElementById('bh-theme-custom-css');
      if (!customCssEl) {
        customCssEl = document.createElement('style');
        customCssEl.id = 'bh-theme-custom-css';
        document.head.appendChild(customCssEl);
      }
      customCssEl.innerHTML = theme.custom_css || '';
    } else if (event.data.type === 'BH_THEME_HIGHLIGHT') {
      showHighlight(event.data.target, event.data.label);
    } else if (event.data.type === 'BH_THEME_CLEAR_HIGHLIGHT') {
      clearHighlight();
    }
  });

  document.addEventListener('click', clearHighlight);
})();
</script>
{% block scripts_extra %}{% endblock %}
</body>
</html>
